{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {FSTypeChanged = id; endButtonClicked = id; }; 
            CLASS = MountController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                FSTypePopup = NSPopUpButton; 
                configurationView = NSView; 
                endButton = NSButton; 
                fileSystem = id; 
                fsUIController = id; 
                switchButton = NSButton; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            ACTIONS = {FSTypeChanged = id; }; 
            CLASS = QuickMountController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                FSType = NSPopUpButton; 
                name = NSTextField; 
                tabView = NSTabView; 
                volumeName = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}